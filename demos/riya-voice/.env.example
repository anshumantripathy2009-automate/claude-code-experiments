# Google Gemini API key (free tier available)
# Get yours at https://aistudio.google.com/app/apikey
# Same key setup as automations/whatsapp-dental-receptionist/ — the key stays
# server-side in api/chat.js and is never sent to the browser.
GEMINI_API_KEY=your_gemini_api_key_here
